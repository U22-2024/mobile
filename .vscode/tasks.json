{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Update gRPC",
            "type": "shell",
            "command": "docker compose -f docker-compose.script.yml run --rm update-proto"
        },
        {
            "label": "Start FirebaseEmulator",
            "type": "shell",
            "command": "firebase emulators:start"
        }
    ]
}